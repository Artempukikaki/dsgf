const trainers = document.querySelectorAll('.trainer');
trainers.forEach(trainer => {
    trainer.addEventListener('click', () => {
        alert(Выбран ,тренер, $,trainer,querySelector('name'),textContent,);
    });
});
